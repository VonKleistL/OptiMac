# OptiMac Enhanced Requirements
# Core dependencies for enhanced functionality
requests>=2.31.0
rumps>=0.4.0

# Build tools (install when building DMG)
py2app>=0.28.6
pyinstaller>=5.13.0

# Optional enhancements
psutil>=5.9.0
plyer>=2.1.0

# Note: tkinter is built into Python on macOS - no need to install
# Standard library modules used (built into Python):
# - tkinter (GUI framework)
# - subprocess (for running shell commands)
# - threading (for non-blocking operations)
# - os, sys, pathlib (system operations)
# - json (for profile management)
# - datetime (for timestamps)